[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    env: environment and filesystem checks
    dataset: dataset-level contract tests
    dataloader: dataloader creation and behavior tests
    batch: GAN batch contract tests (shape, range, dtype)